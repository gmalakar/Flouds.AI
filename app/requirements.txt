
# Include production requirements (keep a single source of truth)
-r requirements-prod.txt

# ONNX Runtime (CPU): loosened to allow Optimum compatibility
# Use a version compatible with `optimum[onnxruntime]>=2.1.0`.
# If you need GPU support, replace with `onnxruntime-gpu` in your environment.
onnxruntime>=1.18.0,<1.23.0
